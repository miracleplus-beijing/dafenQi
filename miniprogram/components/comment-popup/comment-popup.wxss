/* 评论弹窗样式优化：修复时间戳溢出与排版不合理问题 */

.comment-popup {
  position: fixed;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  z-index: 1000;
}

.popup-mask {
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.45);
}

.popup-content {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  max-height: 80vh;
  background: #ffffff;
  border-top-left-radius: 24rpx;
  border-top-right-radius: 24rpx;
  display: flex;
  flex-direction: column;
}

.popup-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 28rpx;
  border-bottom: 1rpx solid #f0f0f0;
}

.popup-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #1a202c;
}

.popup-close {
  padding: 8rpx;
}

.comment-list {
  flex: 1;
  min-height: 200rpx;
  padding: 16rpx 24rpx 24rpx 24rpx;
  box-sizing: border-box;
}

.comment-item {
  display: flex;
  padding: 20rpx 0;
  border-bottom: 1rpx solid #f7f7f7;
}

.comment-avatar {
  width: 64rpx;
  height: 64rpx;
  border-radius: 50%;
  flex: 0 0 64rpx;
  margin-right: 20rpx;
}

.comment-content {
  flex: 1;
  min-width: 0; /* 关键：允许子项正确收缩并触发省略 */
  display: flex;
  flex-direction: column;
  position: relative;
}

.comment-header {
  display: flex;
  align-items: center;
  min-width: 0;
}

.comment-user {
  flex: 1 1 auto;
  min-width: 0;
  font-size: 26rpx;
  font-weight: 500;
  color: #2d3748;
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis; /* 关键：昵称过长时省略 */
}

.comment-time {
  flex: 0 0 auto; /* 让时间戳根据内容自适应，不与昵称抢宽度 */
  margin-left: 16rpx;
  color: #718096;
  font-size: 22rpx;
  white-space: nowrap; /* 关键：防止时间戳换行 */
}

.comment-text {
  margin-top: 8rpx;
  font-size: 28rpx;
  color: #1a202c;
  line-height: 1.6;
  word-break: break-word; /* 关键：长链接/英文自动换行 */
  white-space: pre-wrap;  /* 保留换行 */
}


.like-btn {
  display: flex;
  align-items: flex-end;
  position: absolute;
  right: 4rpx;
  bottom: 4rpx;
}

.like-btn.liked {
  color: #ed8936;
}

.like-icon {
  margin-right: 6rpx;
}
.like>text {
  font-size: 16rpx;

}
.comment-empty {
  padding: 40rpx 0;
  text-align: center;
  color: #a0aec0;
}

.comment-input-wrapper {
  padding: 16rpx 20rpx calc(16rpx + env(safe-area-inset-bottom));
  display: flex;
  align-items: center;
  gap: 16rpx;
  border-top: 1rpx solid #f0f0f0;
  background: #ffffff;
}

.comment-input {
  flex: 1;
  min-width: 0; /* 允许输入框收缩 */
  height: 72rpx;
  padding: 0 20rpx;
  background: #f7fafc;
  border-radius: 12rpx;
  font-size: 28rpx;
}

.send-btn {
  padding: 0 24rpx;
  height: 72rpx;
  border-radius: 12rpx;
  background: #e2e8f0;
  color: #4a5568;
  display: flex;
  align-items: center;
}

.send-btn.active {
  background: #3182ce;
  color: #ffffff;
}
